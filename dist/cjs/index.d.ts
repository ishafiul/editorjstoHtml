export default class EditorJsToHtml {
    private readonly config;
    private readonly parsers;
    constructor(config?: {}, customs?: {}, embeds?: {});
    parse(EditorJsObject: any): any;
    parseBlock(block: any): any;
}
